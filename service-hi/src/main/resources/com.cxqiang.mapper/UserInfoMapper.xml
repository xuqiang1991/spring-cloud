<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cxqiang.mapper.UserInfoMapper">

  <select id="findUserBaseInfoByNick" resultType="com.cxqiang.entity.Account">
         SELECT u.`password`,u.account,r.role_name
           from sys_user u, sys_user_role_ref rr, sys_role r
          WHERE u.uid = rr.user_id
            and rr.role_id = r.id
            and a.account = #{userName}
  </select>
</mapper>